## 11. 원시 값과 객체의 비교

자바스크립트의 데이터 타입은 크게 **원시 타입**과 **객체 타입**으로 나뉜다. 둘은 다음 세 가지 측면에서 다르다.

| 구분 | 원시 값 | 객체 |
|------|---------|------|
| 변경 가능 여부 | 변경 불가능한 값 | 변경 가능한 값 |
| 변수에 저장되는 값 | 실제 값 | 참조 값 |
| 다른 변수에 할당 | 원시 값이 복사됨 | 참조 값이 복사됨 |

## 11.1 원시 값

### 11.1.1 변경 불가능한 값

원시 값은 변경 불가능한 값이다. 여기서 중요한 점은 **변수가 아니라 값이 변경 불가능하다**는 것이다.

```js
var score = 80;
score = 90;
```

위 코드는 숫자 값 `80` 자체를 `90`으로 바꾸는 것이 아니다. 새로운 숫자 값 `90`을 만들고, 변수 `score`가 새 값을 가리키도록 바꾸는 것이다.

즉, 변수는 재할당할 수 있지만 원시 값 자체는 변경할 수 없다.

```js
const o = {};

// const는 재할당이 금지된 변수일 뿐,
// 객체 자체의 변경을 막지는 않는다.
o.a = 1;

console.log(o); // { a: 1 }
```

- 상수: 재할당이 금지된 변수
- 불변성: 값 자체를 변경할 수 없는 성질

원시 값은 불변성을 가지므로 예기치 않은 변경으로부터 안전하고, 데이터의 신뢰성을 보장한다.

### 11.1.2 문자열과 불변성

문자열은 원시 값이며 변경 불가능하다.

```js
var str = "Hello";
str = "world";
```

위 코드는 `"Hello"` 문자열을 수정하는 것이 아니라, 새로운 문자열 `"world"`를 만들고 `str`이 그것을 가리키도록 바꾸는 것이다.

문자열은 배열처럼 인덱스로 각 문자에 접근할 수 있다.

```js
var str = "string";

console.log(str[0]);           // s
console.log(str.length);       // 6
console.log(str.toUpperCase()); // STRING
```

하지만 문자열은 원시 값이므로 일부 문자를 직접 변경할 수 없다.

```js
var str = "string";

str[0] = "S";

console.log(str); // string
```

문자열이 객체처럼 동작하는 이유는 원시 값을 객체처럼 사용할 때 일시적으로 래퍼 객체가 생성되기 때문이다.

### 11.1.3 값에 의한 전달

원시 값을 갖는 변수를 다른 변수에 할당하면 원시 값이 복사되어 전달된다.

```js
var score = 80;
var copy = score;

console.log(score, copy);      // 80 80
console.log(score === copy);   // true

score = 100;

console.log(score, copy);      // 100 80
console.log(score === copy);   // false
```

`copy = score`에서 `score`의 값 `80`이 `copy`에 복사된다. 이후 `score`를 재할당해도 `copy`에는 영향을 주지 않는다.

중요한 점:

- 원시 값은 서로 독립된 값처럼 동작한다.
- 한쪽 변수를 재할당해도 다른 변수에 영향을 주지 않는다.
- “값에 의한 전달”이라는 표현은 엄밀히 말하면 메모리 주소 전달 방식까지 포함하면 단순하지 않지만, 결과적으로 원시 값은 서로 간섭하지 않는다.

## 11.2 객체

객체는 프로퍼티 개수가 정해져 있지 않고, 동적으로 추가/삭제할 수 있다. 프로퍼티 값에도 제약이 없다.

따라서 객체는 원시 값처럼 필요한 메모리 크기를 미리 단순하게 정하기 어렵고, 관리 방식도 더 복잡하다.

### 11.2.1 변경 가능한 값

객체는 변경 가능한 값이다.

```js
var person = {
  name: "Lee"
};
```

객체를 변수에 할당하면 변수에는 객체 자체가 아니라 **참조 값**이 저장된다. 참조 값은 실제 객체가 저장된 메모리 공간을 가리키는 값이다.

```js
var person = {
  name: "Lee"
};

console.log(person); // { name: "Lee" }
```

객체는 재할당 없이 직접 변경할 수 있다.

```js
var person = {
  name: "Lee"
};

person.name = "Kim";
person.address = "Seoul";

console.log(person); // { name: "Kim", address: "Seoul" }
```

객체가 변경 가능한 값으로 설계된 이유:

- 객체는 크기가 일정하지 않다.
- 프로퍼티가 동적으로 추가/삭제될 수 있다.
- 객체를 매번 복사하면 메모리와 성능 비용이 크다.

하지만 이 구조에는 단점이 있다. 여러 식별자가 하나의 객체를 공유할 수 있고, 이로 인해 한쪽의 변경이 다른 쪽에 영향을 줄 수 있다.

### 얕은 복사와 깊은 복사

객체 안에 객체가 중첩되어 있을 때, 복사 방식은 크게 두 가지로 나뉜다.

- 얕은 복사: 한 단계만 복사하고 중첩 객체는 참조를 공유
- 깊은 복사: 중첩 객체까지 모두 복사

```js
const o = { x: { y: 1 } };

const c1 = { ...o };

console.log(c1 === o);     // false
console.log(c1.x === o.x); // true
```

위 예제에서 `c1`과 `o`는 다른 객체지만, 내부 객체 `x`는 같은 객체를 공유한다. 이것이 얕은 복사다.

깊은 복사는 중첩 객체까지 모두 새로 복사한다.

```js
const o = { x: { y: 1 } };
const c2 = structuredClone(o);

console.log(c2 === o);     // false
console.log(c2.x === o.x); // false
```

## 11.2.2 참조에 의한 전달

객체를 가리키는 변수를 다른 변수에 할당하면 참조 값이 복사된다.

```js
var person = {
  name: "Lee"
};

var copy = person;
```

이때 `person`과 `copy`는 같은 객체를 가리킨다.

```js
var person = {
  name: "Lee"
};

var copy = person;

console.log(copy === person); // true

copy.name = "Kim";
person.address = "Seoul";

console.log(person); // { name: "Kim", address: "Seoul" }
console.log(copy);   // { name: "Kim", address: "Seoul" }
```

객체를 공유하면 한쪽에서 프로퍼티를 변경했을 때 다른 쪽에서도 변경된 결과가 보인다.

정리하면:

- 원시 값 할당: 값이 복사되어 서로 독립적으로 동작
- 객체 할당: 참조 값이 복사되어 같은 객체를 공유

단, 자바스크립트에는 다른 언어의 포인터 기반 “참조에 의한 전달”과 완전히 같은 개념은 없다. 실제로는 변수에 저장된 값(원시 값 또는 참조 값)이 복사되어 전달된다고 이해하는 것이 더 정확하다.

## 객체 비교

객체를 비교할 때는 객체의 내용이 아니라 참조 값을 비교한다.

```js
var person1 = {
  name: "Lee"
};

var person2 = {
  name: "Lee"
};

console.log(person1 === person2);       // false
console.log(person1.name === person2.name); // true
```

`person1`과 `person2`는 내용은 같지만 서로 다른 객체다. 따라서 참조 값이 다르므로 `false`다.

반면 `person1.name`과 `person2.name`은 모두 원시 값 `"Lee"`로 평가되므로 `true`다.

---

## 내용 정리

- 원시 값은 변경 불가능하고, 객체는 변경 가능하다.
- 원시 값을 변수에 할당하면 실제 값이 저장된다.
- 객체를 변수에 할당하면 객체 자체가 아니라 참조 값이 저장된다.
- 원시 값을 다른 변수에 할당하면 값이 복사되어 서로 영향을 주지 않는다.
- 객체를 다른 변수에 할당하면 참조 값이 복사되어 같은 객체를 공유한다.
- 문자열은 원시 값이므로 일부 문자를 직접 변경할 수 없다.
- 얕은 복사는 중첩 객체의 참조를 공유하고, 깊은 복사는 중첩 객체까지 복사한다.
- 객체 비교는 내용 비교가 아니라 참조 값 비교다.

## 의문점 제기

1. “원시 값은 변경 불가능하지만 변수는 재할당 가능하다”를 헷갈리지 않게 설명하는 가장 좋은 예시는 무엇일까?
2. `const`로 선언한 객체의 프로퍼티 변경이 가능한 점은 초보자에게 어떤 오해를 만들 수 있을까?
3. 객체 공유로 인한 사이드 이펙트를 줄이려면 어떤 상황에서 복사를 선택해야 할까?
4. 얕은 복사와 깊은 복사의 비용 차이는 실제 서비스 코드에서 언제 문제가 될까?
5. `structuredClone`, 스프레드 문법, 라이브러리의 `cloneDeep`은 각각 어떤 기준으로 선택해야 할까?
6. 객체를 직접 변경하는 방식과 불변성을 유지하는 방식은 어떤 트레이드오프가 있을까?
7. 객체의 내용이 같은지 비교하려면 `===` 대신 어떤 방식이 필요할까?
8. 상태 관리 라이브러리에서 불변성을 강조하는 이유는 객체의 참조 공유 문제와 어떻게 연결될까?

## 답변

1. **“값은 불변, 변수는 재할당”을 가장 잘 보여주는 예시**
   - 핵심은 “기존 값이 바뀌는 게 아니라 **새 값이 만들어지고 변수가 가리키는 대상이 바뀐다**”를 보여주는 것이다.
   - 예를 들어:
     - `let a = 80; a = 90;`은 **80을 수정**하는 게 아니라 **90을 새로 만들고 a를 90으로 바꾸는 것**.
     - 문자열도 동일하게 `let s = "Hello"; s = "world";`는 `"Hello"`를 편집하는 게 아니라 `"world"`로 **재할당**이다.
   - 반대로 객체 예시(`const o = {}; o.a = 1`)를 같이 보여주면 “변수 재할당”과 “객체 내부 변경”이 다른 층위라는 점이 더 선명해진다.

2. **`const` 객체 프로퍼티 변경이 만드는 오해**
   - 흔한 오해: `const`는 “객체를 얼린다(freeze)” 혹은 “값이 절대 안 바뀐다”로 착각한다.
   - 실제로 `const`는 **바인딩(변수에 담긴 참조 값)의 재할당만 금지**하고, 그 참조가 가리키는 **객체의 내부 변경은 막지 않는다**.
   - 그래서 초보자는 “왜 const인데 바뀌지?” 또는 반대로 “const면 안전하겠지”라고 생각하고 객체를 공유하다가 사이드 이펙트를 겪기 쉽다.

3. **사이드 이펙트를 줄이기 위해 언제 ‘복사’를 선택할까**
   - **복사를 고려해야 하는 신호**
     - 같은 객체를 **여러 곳(컴포넌트/모듈/함수 경계)**에서 참조한다.
     - 함수가 인자로 받은 객체를 수정하면 호출자 상태까지 바뀌어 **추적이 어려워진다**.
     - 변경 이력/비교(이전 상태 vs 다음 상태)가 필요하다.
   - 실무 기준: “공유될 가능성이 있는 데이터”를 경계에서 받으면, **수정 전 복사(또는 새 객체 생성)** 쪽이 안전한 기본값이다.

4. **얕은 복사 vs 깊은 복사 비용이 문제가 되는 순간**
   - **깊은 복사**는 중첩 구조 전체를 복제하므로, 다음에서 병목이 되기 쉽다.
     - 상태 트리(큰 JSON) 전체를 자주 복제하는 경우(렌더/이벤트마다).
     - 리스트/테이블처럼 항목 수가 많고, 각 항목이 중첩 객체를 가진 경우.
     - 네트워크 응답을 그대로 깊은 복사해서 여러 번 가공하는 파이프라인.
   - 반대로 **얕은 복사**는 빠르지만, 중첩 객체 참조를 공유해서 “내가 복사했다고 생각했는데 내부가 같이 바뀌는” 버그가 난다.

5. **`structuredClone` / 스프레드 / `cloneDeep` 선택 기준**
   - **스프레드(`{...obj}`, `[...arr]`)**: “한 단계만 새로” 만들면 충분할 때(얕은 복사). 가장 가볍고 흔한 선택.
   - **`structuredClone`**: “진짜로 중첩까지 분리된 복사”가 필요하고, 데이터가 **구조화 복제 가능한 타입**(plain object/array 등) 위주일 때 표준 API로 깔끔하다.
   - **`cloneDeep`(라이브러리)**: 복잡한 데이터 구조/레거시 타입/호환성 요구가 있거나, 팀 규약으로 일관되게 깊은 복사를 제공해야 할 때 선택한다.
   - 기본 전략은 “얕은 복사를 기본으로 두고, 필요할 때만 깊은 복사”가 비용 면에서 유리하다.

6. **직접 변경(mutating) vs 불변성(immutable) 트레이드오프**
   - **직접 변경 장점**: 간단하고 빠를 수 있다(불필요한 객체 생성 감소).
   - **직접 변경 단점**: 공유 참조가 있으면 사이드 이펙트가 퍼지고, “언제/어디서 바뀌었는지” 추적이 어려워진다.
   - **불변성 장점**: 변경이 “새 값 생성”으로 일어나서 비교/디버깅/롤백이 쉬워지고, 변경 감지(참조 비교)로 최적화하기 좋다.
   - **불변성 단점**: 객체 생성/복사 비용이 늘 수 있고, 깊은 구조에서 실수하면 코드가 장황해질 수 있다.

7. **객체 ‘내용’ 비교는 어떻게 하나**
   - `===`는 참조(주소 개념)에 대한 비교라서 “내용이 같은지”는 판별하지 못한다.
   - 내용 비교가 필요하면 보통 다음 중 하나를 고른다.
     - **도메인 키 기반 비교**: 예) `user.id`처럼 고유 식별자를 비교(실무에서 가장 선호).
     - **깊은 동등성 비교(deep equality)**: 중첩까지 값이 같은지 검사(유틸/라이브러리 사용).
     - **직렬화 기반 비교**: `JSON.stringify` 비교는 순서/타입 제약이 커서 제한적으로만 사용.

8. **상태 관리에서 불변성을 강조하는 이유와 ‘참조 공유’의 연결**
   - 상태 관리에서는 “무엇이 바뀌었는지”를 빨리 알아야 하는데, 객체를 직접 변경하면 이전 상태와 다음 상태가 **같은 참조를 공유**해서 변경 감지가 어려워진다.
   - 불변성을 유지하면 업데이트가 “새 객체(새 참조)”를 만들기 때문에,
     - **참조 비교만으로 변경 여부를 감지**할 수 있고,
     - 이전 상태를 안전하게 보존할 수 있어 타임트래블/디버깅/메모이제이션에 유리하다.
