## 15. `let`, `const` 키워드와 블록 레벨 스코프

ES5까지 변수를 선언하는 유일한 방법은 `var` 키워드였다. 하지만 `var`는 중복 선언, 함수 레벨 스코프, 변수 호이스팅으로 인해 의도치 않은 문제를 만들 수 있다. ES6에서는 이를 보완하기 위해 `let`, `const`가 도입되었다.

## 15.1 `var` 키워드로 선언한 변수의 문제점

### 15.1.1 변수 중복 선언 허용

`var` 키워드로 선언한 변수는 같은 스코프 안에서 중복 선언할 수 있다.

```js
var x = 1;
var y = 1;

var x = 100;
var y;

console.log(x); // 100
console.log(y); // 1
```

초기화문이 있는 중복 선언은 기존 변수에 값을 다시 할당하는 것처럼 동작한다. 초기화문이 없는 중복 선언은 무시된다.

이 때문에 이미 선언된 변수를 모르고 다시 선언하면 의도치 않게 값이 바뀔 수 있다.

### 15.1.2 함수 레벨 스코프

`var`는 함수 몸체만 지역 스코프로 인정한다. `if`, `for` 같은 블록은 지역 스코프를 만들지 않는다.

```js
var x = 1;

if (true) {
  var x = 10;
}

console.log(x); // 10
```

`for` 문에서도 같은 문제가 생긴다.

```js
var i = 10;

for (var i = 0; i < 5; i++) {
  console.log(i); // 0 1 2 3 4
}

console.log(i); // 5
```

`var`의 함수 레벨 스코프는 전역 변수를 남발하거나 의도치 않은 중복 선언을 만들 가능성을 높인다.

### 15.1.3 변수 호이스팅

`var`로 선언한 변수는 변수 선언이 스코프의 선두로 끌어올려진 것처럼 동작한다. 선언문 이전에 참조할 수 있지만, 할당 전에는 `undefined`다.

```js
console.log(foo); // undefined

foo = 123;

console.log(foo); // 123

var foo;
```

변수 선언문 이전에 변수를 참조할 수 있다는 점은 코드 흐름을 읽기 어렵게 만들고 오류 가능성을 높인다.

## 15.2 `let` 키워드

`let`은 `var`의 단점을 보완하기 위해 도입된 변수 선언 키워드다.

### 15.2.1 변수 중복 선언 금지

`let`은 같은 스코프에서 중복 선언을 허용하지 않는다.

```js
let bar = 123;
// let bar = 456; // SyntaxError
```

### 15.2.2 블록 레벨 스코프

`let`으로 선언한 변수는 모든 코드 블록을 지역 스코프로 인정한다.

```js
let foo = 1;

{
  let foo = 2;
  let bar = 3;
}

console.log(foo); // 1
// console.log(bar); // ReferenceError
```

`if`, `for`, `while`, `try/catch` 등의 블록 안에서 선언한 `let` 변수는 해당 블록 내부에서만 유효하다.

```js
let i = 10;

function foo() {
  let i = 100;

  for (let i = 1; i < 3; i++) {
    console.log(i); // 1 2
  }

  console.log(i); // 100
}

foo();
console.log(i); // 10
```

### 15.2.3 변수 호이스팅과 TDZ

`let`으로 선언한 변수는 호이스팅이 발생하지 않는 것처럼 동작한다.

```js
// console.log(foo); // ReferenceError
let foo;
```

하지만 실제로는 `let`도 호이스팅된다. 다만 `var`와 달리 선언 단계와 초기화 단계가 분리되어 있다.

- `var`: 선언 단계와 초기화 단계가 런타임 이전에 함께 실행됨
- `let`: 선언 단계는 런타임 이전에 실행되지만, 초기화 단계는 변수 선언문에 도달했을 때 실행됨

스코프 시작 지점부터 초기화 단계 전까지 변수를 참조할 수 없는 구간을 **일시적 사각지대(TDZ, Temporal Dead Zone)** 라고 한다.

```js
// TDZ
// console.log(foo); // ReferenceError

let foo;
console.log(foo); // undefined

foo = 1;
console.log(foo); // 1
```

`let`도 호이스팅된다는 점은 다음 예제로 확인할 수 있다.

```js
let foo = 1;

{
  // 전역 foo를 참조하지 않고, 블록 내부의 let foo가 호이스팅되어 TDZ에 걸린다.
  // console.log(foo); // ReferenceError
  let foo = 2;
}
```

자바스크립트의 모든 선언(`var`, `let`, `const`, `function`, `class` 등)은 호이스팅된다. 단, `let`, `const`, `class`는 TDZ 때문에 호이스팅이 발생하지 않는 것처럼 보인다.

### 15.2.4 전역 객체와 `let`

브라우저 환경에서 `var`로 선언한 전역 변수는 전역 객체 `window`의 프로퍼티가 된다.

```js
var x = 1;

console.log(window.x); // 1
console.log(x);        // 1
```

하지만 `let`으로 선언한 전역 변수는 전역 객체의 프로퍼티가 아니다.

```js
let x = 1;

console.log(window.x); // undefined
console.log(x);        // 1
```

`let` 전역 변수는 전역 렉시컬 환경의 선언적 환경 레코드에 존재한다.

## 15.3 `const` 키워드

`const`는 상수를 선언하기 위해 사용한다. 대부분의 특징은 `let`과 동일하지만, 중요한 차이가 있다.

### 15.3.1 선언과 초기화

`const`는 반드시 선언과 동시에 초기화해야 한다.

```js
const foo = 1;
```

```js
// const bar; // SyntaxError
```

`const`도 블록 레벨 스코프를 가지며, TDZ로 인해 호이스팅이 발생하지 않는 것처럼 동작한다.

```js
{
  // console.log(foo); // ReferenceError
  const foo = 1;
  console.log(foo); // 1
}

// console.log(foo); // ReferenceError
```

### 15.3.2 재할당 금지

`const`로 선언한 변수는 재할당할 수 없다.

```js
const foo = 1;

// foo = 2; // TypeError
```

### 15.3.3 상수

`const`에 원시 값을 할당하면 재할당할 수 없고, 원시 값 자체도 변경 불가능하므로 상수처럼 사용할 수 있다.

매직 넘버 대신 의미 있는 상수를 사용하면 가독성과 유지보수성이 좋아진다.

```js
const TAX_RATE = 0.1;

let preTaxPrice = 100;
let afterTaxPrice = preTaxPrice + (preTaxPrice * TAX_RATE);

console.log(afterTaxPrice); // 110
```

상수 이름은 보통 대문자와 언더스코어를 사용한다.

### 15.3.4 `const` 키워드와 객체

`const`는 재할당을 금지할 뿐, 불변을 의미하지 않는다.

`const`로 선언한 변수에 객체를 할당하면 객체 자체는 변경할 수 있다.

```js
const person = {
  name: "Lee"
};

person.name = "Kim";

console.log(person); // { name: "Kim" }
```

객체의 프로퍼티를 변경해도 변수에 저장된 참조 값은 바뀌지 않으므로 재할당이 아니다.

## 15.4 `var` vs `let` vs `const`

권장 사용 방식:

1. ES6 이후에는 `var`를 사용하지 않는다.
2. 재할당이 필요한 경우에만 `let`을 사용한다.
3. 재할당이 필요 없는 값은 `const`를 사용한다.
4. 일단 `const`로 선언하고, 정말 재할당이 필요할 때 `let`으로 바꾼다.

```js
const user = {
  name: "Lee"
};

let count = 0;

count++;
```

`const`를 기본으로 사용하면 의도치 않은 재할당을 막을 수 있어 더 안전하다.

---

## 내용 정리

- `var`는 중복 선언을 허용하고 함수 레벨 스코프를 따르며, 호이스팅으로 `undefined` 참조가 가능하다.
- `let`은 중복 선언을 금지하고 블록 레벨 스코프를 따른다.
- `let`도 호이스팅되지만 TDZ 때문에 선언 전 참조가 불가능하다.
- `var` 전역 변수는 브라우저에서 `window`의 프로퍼티가 되지만, `let` 전역 변수는 그렇지 않다.
- `const`는 선언과 동시에 초기화해야 하며 재할당이 금지된다.
- `const`는 불변을 의미하지 않으므로 객체의 프로퍼티 변경은 가능하다.
- 상수는 의미 있는 이름으로 선언해 매직 넘버를 줄이고 유지보수성을 높인다.
- 기본은 `const`, 재할당이 필요할 때만 `let`, `var`는 지양한다.

## 의문점 제기

1. `let`과 `const`도 호이스팅되는데 “호이스팅되지 않는다”라고 설명하면 어떤 오해가 생길까?
2. TDZ는 실제 코드에서 어떤 버그를 미리 막아주는 역할을 할까?
3. `const` 객체의 프로퍼티 변경을 막으려면 어떤 방법(`Object.freeze`, 불변 라이브러리 등)이 필요할까?
4. 반복문에서 `let i`를 사용하면 클로저 동작에 어떤 차이가 생길까?
5. 전역 `let`이 `window` 프로퍼티가 아닌 점은 브라우저 디버깅에서 어떤 차이를 만들까?
6. 팀 컨벤션으로 `prefer-const`를 강제하면 실제 코드 품질에 어떤 장점이 있을까?
7. 재할당이 필요한 변수와 객체 내부 변경이 필요한 변수를 어떻게 구분해 네이밍하면 좋을까?
8. `var`를 사용하는 레거시 코드를 `let`/`const`로 바꿀 때 가장 조심해야 할 부분은 무엇일까?

## 답변

1. **“호이스팅되지 않는다”라고 설명할 때 생기는 오해**
   - `let/const`는 실제로 **선언 자체는 호이스팅**되는데, TDZ 때문에 “선언 전 접근이 막힌다”.
   - 그래서 “호이스팅이 없다”라고만 말하면
     - 왜 블록 내부에서 같은 이름이 TDZ에 걸리는지(상위 스코프 변수를 못 보고 `ReferenceError`)를 이해하기 어렵고,
     - “그럼 선언이 아예 위로 올라가지 않는다” 같은 잘못된 모델을 갖기 쉽다.
   - 더 정확한 표현은 “**호이스팅은 되지만 TDZ로 인해 선언 전 접근이 불가**”다.

2. **TDZ가 미리 막아주는 버그 유형**
   - **선언 전에 값을 사용하는 실수**를 즉시 `ReferenceError`로 드러내서, 조용히 `undefined`가 퍼지는 문제를 줄인다.
   - 특히 블록 스코프에서:
     - 같은 이름의 변수를 내부에서 선언했는데, 내부 선언 전 로그/연산이 상위 변수를 쓸 거라 착각하는 버그를 막는다.
   - 결과적으로 “초기화 순서가 중요한 코드”에서 오류를 더 빨리 발견하게 해준다.

3. **`const` 객체 프로퍼티 변경을 막는 방법 선택**
   - `const`는 재할당만 막기 때문에 “객체 내부 불변”이 필요하면 별도 조치가 필요하다.
   - 선택지:
     - **`Object.freeze`**: 얕게(1단계) 얼린다. 중첩 객체까지 막으려면 재귀적으로 freeze 해야 한다.
     - **불변 업데이트 패턴**: 직접 변경 대신 복사해서 새 객체를 만들고 교체한다(가장 범용).
     - **불변 라이브러리**: 깊은 구조 업데이트를 안전/간결하게 만들 때 유리(대규모 상태 트리에서).
   - 기준은 “중첩 깊이/업데이트 빈도/팀이 감당할 복잡도”로 잡는 게 현실적이다.

4. **반복문에서 `let i`가 클로저에 주는 차이**
   - `var i`는 함수 레벨 스코프라 반복문이 끝난 뒤에도 “하나의 i”를 공유하기 쉽고, 비동기 콜백에서 마지막 값만 보이는 문제가 흔하다.
   - `let i`는 블록 레벨 스코프라 반복마다 **각 반복의 i가 분리**되어, 콜백이 기대한 값을 더 자연스럽게 캡처한다.
   - 즉, 반복문 + 비동기(타이머/이벤트) 조합에서 `let`이 버그를 크게 줄인다.

5. **전역 `let`이 `window` 프로퍼티가 아니라서 생기는 디버깅 차이**
   - 콘솔에서 `window.x`로는 안 보이는데, 스크립트에서는 `x`로 접근이 되는 “겉보기 불일치”가 생길 수 있다.
   - 전역 `let/const`는 전역 객체가 아니라 **전역 렉시컬 환경**에 놓이므로,
     - 전역 오염(전역 객체 프로퍼티 추가) 관점에서는 더 안전하지만,
     - 레거시 디버깅 습관(`window`로 확인)과는 다르게 보인다.

6. **`prefer-const` 강제가 주는 코드 품질 장점**
   - 의도치 않은 재할당을 줄여서 “변수가 언제 바뀌는지” 추적 비용을 낮춘다.
   - 기본값이 `const`가 되면, 재할당이 필요한 변수만 `let`으로 남아 **변경 가능 지점이 코드에서 눈에 띈다**.
   - 결과적으로 리팩터링 시 부작용이 줄고, 코드 리뷰에서도 변경 범위를 빠르게 파악하기 좋다.

7. **재할당 vs 객체 내부 변경을 네이밍으로 구분하는 방법**
   - 네이밍만으로 100% 강제하긴 어렵지만, “변경 가능성”을 드러내는 규칙은 도움이 된다.
   - 예시 기준:
     - **재할당될 값**: `count`, `index`, `currentUserId`처럼 “흐름에 따라 값이 바뀌는” 의미를 담는다(보통 `let`).
     - **참조는 고정이지만 내부가 변할 수 있는 객체**: `state`, `cache`, `config`처럼 역할을 드러내고, 가능하면 내부 변경을 줄이거나 업데이트 함수로만 변경한다(`const` + 불변 업데이트 추천).
     - **상수**: `TAX_RATE`, `MAX_RETRY`처럼 대문자 스네이크로 “절대 재할당 없음”을 강하게 표현.

8. **`var` → `let/const` 마이그레이션에서 가장 조심할 점**
   - **스코프 차이**: `var`는 함수 레벨, `let/const`는 블록 레벨이라, 블록 밖에서 참조하던 변수가 더 이상 보이지 않을 수 있다.
   - **TDZ로 인한 ReferenceError**: 이전에는 `undefined`로 흘러가던 코드가 선언 전 접근에서 바로 터질 수 있다(실제로는 “잠재 버그가 드러난 것”인 경우가 많다).
   - **반복문/클로저 동작 변화**: `for (var i...)` 패턴이 의도적으로 “공유 i”를 활용하던 코드라면 의미가 바뀔 수 있다.
   - 결론: 단순 치환보다 “스코프 경계(블록/루프/try/catch) + 선언/사용 순서”를 같이 점검하는 게 안전하다.
